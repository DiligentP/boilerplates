# Portainer 대체 Docker 웹 UI인 Dockerhand
services:
  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand
    ports:
      - "3000:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - dockhand_data:/data
    restart: unless-stopped

volumes:
  dockhand_data: